# 検証ページ - ワイヤーフレーム

**ページパス**: `/verify`
**ファイル**: `src/app/verify/page.tsx`

---

## 画面構成図

```
┌─────────────────────────────────────────────────────────────────┐
│                         Header                                   │
│  [Logo] symproof-tree    [Home] [Tree] [Portfolio] [Verify]    │
│                                          [Symbol Testnet 🟢]    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                     ページタイトル                        │   │
│  │                                                           │   │
│  │              ブロックチェーン証明検証                    │   │
│  │        学習記録の真正性をSymbol Testnetで確認           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                  検証方法の説明                          │   │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━      │   │
│  │                                                           │   │
│  │  このページでは、学習記録がブロックチェーン上に正しく   │   │
│  │  記録されているかを検証できます。                        │   │
│  │                                                           │   │
│  │  🔍 検証できること:                                      │   │
│  │  • トランザクションがSymbol Testnetに存在するか         │   │
│  │  • 記録内容が改ざんされていないか (ハッシュ一致)        │   │
│  │  • タイムスタンプが一致しているか                        │   │
│  │                                                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       VerifyPanel                                │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  検証方法を選択                                            │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │  ( ) トランザクションハッシュで検証                       │ │
│  │  (●) 記録IDで検証                                         │ │
│  │                                                             │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  トランザクションハッシュ入力                              │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │  📝 トランザクションハッシュを入力してください:           │ │
│  │                                                             │ │
│  │  ┌─────────────────────────────────────────────────────┐ │ │
│  │  │ E7A4B9C2D1F3A5B7C9D2E4F6A8B0C2D4E6F8A0B2C4D6E8F0... │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  │                                                             │ │
│  │  💡 ヒント: トランザクションハッシュは64文字の16進数です │ │
│  │                                                             │ │
│  │              [クリア]           [検証する]                │ │
│  │                                                             │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     検証結果表示エリア                           │
│  (検証ボタンクリック後に表示)                                   │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  検証中...                                                  │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │         [ローディングスピナー 🔄]                         │ │
│  │                                                             │ │
│  │  1. Symbol Testnetからトランザクションを取得中...  [⏳]   │ │
│  │  2. メッセージペイロードを解析中...                 [⏳]   │ │
│  │  3. ローカル記録を検索中...                         [⏳]   │ │
│  │  4. コンテンツハッシュを再計算中...                 [⏳]   │ │
│  │  5. ハッシュを比較中...                             [⏳]   │ │
│  │                                                             │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                   検証成功時の表示 (ProofCard)                  │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  検証結果: ✅ 検証成功                                     │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │  すべての検証項目が合格しました。この学習記録は           │ │
│  │  改ざんされておらず、真正なものであることが確認されました│ │
│  │                                                             │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │  📋 検証項目:                                              │ │
│  │                                                             │ │
│  │  ✅ トランザクション存在確認                              │ │
│  │     トランザクションがSymbol Testnetに存在します          │ │
│  │                                                             │ │
│  │  ✅ コンテンツハッシュ一致                                │ │
│  │     ブロックチェーン上のハッシュとローカル計算値が一致    │ │
│  │     • ブロックチェーン: a1b2c3d4e5f6...                   │ │
│  │     • ローカル計算: a1b2c3d4e5f6...                       │ │
│  │                                                             │ │
│  │  ✅ タイムスタンプ一致                                    │ │
│  │     記録時刻がブロックチェーンと一致しています            │ │
│  │     • 記録時刻: 2024-11-01 15:15:00                       │ │
│  │                                                             │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │  📊 ブロックチェーン情報:                                 │ │
│  │                                                             │ │
│  │  🔗 トランザクションハッシュ:                             │ │
│  │     E7A4B9C2D1F3A5B7C9D2E4F6A8B0C2D4E6F8A0B2C4D6E8F0...   │ │
│  │                                                             │ │
│  │  📦 ブロック高: 1234567                                    │ │
│  │  ⏰ ブロック時刻: 2024-11-01 15:15:30                     │ │
│  │  🔐 確認数: 100+ (確定)                                   │ │
│  │                                                             │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │  📄 記録詳細:                                              │ │
│  │                                                             │ │
│  │  タイトル: Routing: Defining Routes | Next.js            │ │
│  │  学習時間: 45分                                            │ │
│  │  理解度: 4/5                                               │ │
│  │  タグ: Next.js, web-development, ルーティング             │ │
│  │                                                             │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │  [Symbol Explorerで確認]  [新しい検証]  [詳細を表示]    │ │
│  │                                                             │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                   検証失敗時の表示 (ProofCard)                  │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  検証結果: ❌ 検証失敗                                     │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │  ⚠️  警告: 一部の検証項目が失敗しました                  │ │
│  │  この記録は改ざんされている可能性があります               │ │
│  │                                                             │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │  📋 検証項目:                                              │ │
│  │                                                             │ │
│  │  ✅ トランザクション存在確認                              │ │
│  │     トランザクションがSymbol Testnetに存在します          │ │
│  │                                                             │ │
│  │  ❌ コンテンツハッシュ不一致                              │ │
│  │     ブロックチェーン上のハッシュとローカル計算値が不一致  │ │
│  │     • ブロックチェーン: a1b2c3d4e5f6...                   │ │
│  │     • ローカル計算: b2c3d4e5f6a7...                       │ │
│  │     ⚠️  記録内容が改ざんされている可能性があります        │ │
│  │                                                             │ │
│  │  ✅ タイムスタンプ一致                                    │ │
│  │     記録時刻がブロックチェーンと一致しています            │ │
│  │                                                             │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │  💡 考えられる原因:                                        │ │
│  │  • ローカルの記録データが改ざんされている                │ │
│  │  • ブロックチェーン上の記録とローカル記録が異なる        │ │
│  │  • システムエラーによる不整合                            │ │
│  │                                                             │ │
│  │  [Symbol Explorerで確認]  [再検証]  [サポートに連絡]    │ │
│  │                                                             │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       エラー表示                                 │
│  (トランザクションが見つからない場合)                           │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  ❌ トランザクションが見つかりません                       │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │  入力されたトランザクションハッシュに対応する              │ │
│  │  トランザクションがSymbol Testnet上に見つかりませんでした │ │
│  │                                                             │ │
│  │  入力値: E7A4B9C2D1F3A5B7C9D2E4F6A8B0C2D4E6F8A0B2...      │ │
│  │                                                             │ │
│  │  💡 以下をご確認ください:                                 │ │
│  │  • ハッシュが正しく入力されているか                       │ │
│  │  • 64文字の16進数であるか                                │ │
│  │  • コピー時に余分なスペースが含まれていないか            │ │
│  │  • ネットワークがTestnetに設定されているか               │ │
│  │                                                             │ │
│  │              [もう一度試す]  [ホームに戻る]              │ │
│  │                                                             │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     使い方ガイド                                 │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  📚 検証方法ガイド                                         │ │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │ │
│  │                                                             │ │
│  │  🔸 方法1: トランザクションハッシュで検証                │ │
│  │                                                             │ │
│  │  1. 確認済み学習記録からトランザクションハッシュをコピー  │ │
│  │  2. 上記の入力フィールドにペースト                        │ │
│  │  3. 「検証する」ボタンをクリック                          │ │
│  │  4. 検証結果を確認                                        │ │
│  │                                                             │ │
│  │  🔸 方法2: 記録IDで検証                                   │ │
│  │                                                             │ │
│  │  1. 学習記録のIDを入力                                    │ │
│  │  2. システムが自動的にトランザクションハッシュを検索      │ │
│  │  3. 検証を実行                                            │ │
│  │                                                             │ │
│  │  🔸 Symbol Explorerでの確認                               │ │
│  │                                                             │ │
│  │  検証結果の「Symbol Explorerで確認」ボタンをクリックすると│ │
│  │  公式のブロックチェーンエクスプローラーで詳細情報を       │ │
│  │  確認できます。                                            │ │
│  │                                                             │ │
│  │  URL: https://testnet.symbol.fyi/transactions/[txHash]    │ │
│  │                                                             │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## コンポーネント構成

### 1. **Header** (`src/components/Header.tsx`)

**役割**: グローバルナビゲーション（全ページ共通）

---

### 2. **ページタイトルセクション**

**役割**: ページの目的説明

**表示内容**:
- タイトル: "ブロックチェーン証明検証"
- サブタイトル: "学習記録の真正性をSymbol Testnetで確認"

---

### 3. **検証方法の説明セクション**

**役割**: ユーザーに検証機能の概要を伝える

**表示内容**:
- 検証できること（3項目）:
  - トランザクションの存在確認
  - ハッシュ一致確認
  - タイムスタンプ確認

---

### 4. **VerifyPanel** (`src/components/VerifyPanel.tsx`)

**役割**: 検証方法の選択と入力

**Props**:
```typescript
interface VerifyPanelProps {
  onVerify: (input: string, method: 'txHash' | 'recordId') => void;
}
```

#### 検証方法選択

**オプション**:
- ( ) トランザクションハッシュで検証
- (●) 記録IDで検証

**状態管理**:
```typescript
const [verifyMethod, setVerifyMethod] = useState<'txHash' | 'recordId'>('txHash');
```

#### 入力フィールド

**トランザクションハッシュ入力**:
- プレースホルダー: "E7A4B9C2D1F3A5B7..."
- バリデーション: 64文字の16進数
- 自動トリム（前後の空白削除）

**記録ID入力** (方法2選択時):
- プレースホルダー: "a1b2c3d4-e5f6-..."
- バリデーション: UUID v4形式

**ヒントメッセージ**:
- "💡 ヒント: トランザクションハッシュは64文字の16進数です"

#### アクション

**クリアボタン**:
- 入力フィールドをクリア
- 検証結果をリセット

**検証するボタン**:
- バリデーション実行
- 検証処理開始
- ローディング表示

**バリデーション**:
```typescript
const validateTxHash = (hash: string): boolean => {
  const trimmed = hash.trim();
  return /^[A-F0-9]{64}$/i.test(trimmed);
};

const validateRecordId = (id: string): boolean => {
  const uuidRegex = /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
  return uuidRegex.test(id);
};
```

---

### 5. **検証プロセス表示**

**役割**: 検証中の進行状況を表示

**表示内容** (各ステップ):
1. Symbol Testnetからトランザクションを取得中... [⏳]
2. メッセージペイロードを解析中... [⏳]
3. ローカル記録を検索中... [⏳]
4. コンテンツハッシュを再計算中... [⏳]
5. ハッシュを比較中... [⏳]

**完了時**: [⏳] → [✅]

**ローディングスピナー**: 中央に回転アニメーション

---

### 6. **ProofCard** (`src/components/ProofCard.tsx`)

**役割**: 検証結果の詳細表示

**Props**:
```typescript
interface ProofCardProps {
  verificationResult: VerificationResult;
  onReset: () => void;
}

interface VerificationResult {
  valid: boolean;
  txHash: string;
  blockHeight: number;
  timestamp: string;
  checks: {
    transactionExists: boolean;
    hashMatch: boolean;
    timestampMatch: boolean;
  };
  details: {
    blockchainHash: string;
    localHash: string;
    title: string;
    duration: number;
    understanding: number;
    tags: string[];
  };
}
```

#### 検証成功時

**表示内容**:
- ✅ 検証成功ヘッダー（緑背景）
- 成功メッセージ
- 検証項目（すべて ✅）
  - トランザクション存在確認
  - コンテンツハッシュ一致
  - タイムスタンプ一致
- ブロックチェーン情報
  - トランザクションハッシュ（コピー可能）
  - ブロック高
  - ブロック時刻
  - 確認数
- 記録詳細
  - タイトル
  - 学習時間
  - 理解度
  - タグ

**アクション**:
- "Symbol Explorerで確認": 外部リンク（新しいタブ）
- "新しい検証": フォームをリセット
- "詳細を表示": 記録の完全な詳細モーダル

#### 検証失敗時

**表示内容**:
- ❌ 検証失敗ヘッダー（赤背景）
- 警告メッセージ
- 検証項目（失敗項目は ❌）
  - 失敗理由の詳細説明
  - ハッシュの比較表示（不一致箇所）
- 考えられる原因（箇条書き）

**アクション**:
- "Symbol Explorerで確認": 外部リンク
- "再検証": 同じハッシュで再度検証
- "サポートに連絡": サポートページへのリンク

---

### 7. **エラー表示**

**役割**: トランザクションが見つからない場合のエラーメッセージ

**表示内容**:
- ❌ トランザクションが見つかりませんヘッダー
- エラーメッセージ
- 入力値の表示
- 確認事項（チェックリスト）
  - ハッシュが正しいか
  - 64文字の16進数か
  - 余分なスペースがないか
  - Testnetに設定されているか

**アクション**:
- "もう一度試す": 入力フォームに戻る
- "ホームに戻る": ホームページに遷移

---

### 8. **使い方ガイドセクション**

**役割**: 検証方法の詳細ガイド

**表示内容**:
- 🔸 方法1: トランザクションハッシュで検証（4ステップ）
- 🔸 方法2: 記録IDで検証（3ステップ）
- 🔸 Symbol Explorerでの確認方法

---

## ページロジック

### 検証フロー

```typescript
// src/app/verify/page.tsx

const VerifyPage = () => {
  const [txHash, setTxHash] = useState('');
  const [verifyMethod, setVerifyMethod] = useState<'txHash' | 'recordId'>('txHash');
  const [isVerifying, setIsVerifying] = useState(false);
  const [verificationStep, setVerificationStep] = useState(0);
  const [result, setResult] = useState<VerificationResult | null>(null);
  const [error, setError] = useState<string | null>(null);

  const handleVerify = async () => {
    setIsVerifying(true);
    setVerificationStep(0);
    setError(null);

    try {
      // Step 1: トランザクション取得
      setVerificationStep(1);
      const tx = await fetchTransactionFromSymbol(txHash);

      if (!tx) {
        throw new Error('トランザクションが見つかりません');
      }

      // Step 2: ペイロード解析
      setVerificationStep(2);
      const payload = parseTransactionPayload(tx);

      // Step 3: ローカル記録検索
      setVerificationStep(3);
      const localRecord = getLocalRecordById(payload.id);

      if (!localRecord) {
        throw new Error('ローカル記録が見つかりません');
      }

      // Step 4: ハッシュ再計算
      setVerificationStep(4);
      const calculatedHash = await calculateContentHash(localRecord);

      // Step 5: 比較
      setVerificationStep(5);
      const hashMatch = calculatedHash === payload.contentHash;
      const timestampMatch = localRecord.createdAt === payload.timestamp;

      const verificationResult: VerificationResult = {
        valid: hashMatch && timestampMatch,
        txHash: tx.transactionInfo.hash,
        blockHeight: tx.transactionInfo.height,
        timestamp: payload.timestamp,
        checks: {
          transactionExists: true,
          hashMatch,
          timestampMatch,
        },
        details: {
          blockchainHash: payload.contentHash,
          localHash: calculatedHash,
          title: payload.metadata.title,
          duration: payload.metadata.duration,
          understanding: payload.metadata.understanding,
          tags: payload.metadata.tags,
        },
      };

      setResult(verificationResult);

    } catch (err: any) {
      setError(err.message);
    } finally {
      setIsVerifying(false);
    }
  };

  const handleReset = () => {
    setTxHash('');
    setResult(null);
    setError(null);
    setVerificationStep(0);
  };

  return (
    <div>
      <Header />

      <main>
        <PageTitle />
        <VerificationGuide />

        <VerifyPanel
          method={verifyMethod}
          onMethodChange={setVerifyMethod}
          txHash={txHash}
          onTxHashChange={setTxHash}
          onVerify={handleVerify}
          onClear={handleReset}
          disabled={isVerifying}
        />

        {isVerifying && (
          <VerificationProgress step={verificationStep} />
        )}

        {result && (
          <ProofCard
            verificationResult={result}
            onReset={handleReset}
          />
        )}

        {error && (
          <ErrorDisplay
            message={error}
            onRetry={handleReset}
          />
        )}

        <UsageGuide />
      </main>
    </div>
  );
};
```

---

## Symbol SDK統合

### トランザクション取得

```typescript
import { RepositoryFactoryHttp, TransactionGroup } from 'symbol-sdk';

const fetchTransactionFromSymbol = async (txHash: string) => {
  const nodeUrl = process.env.NEXT_PUBLIC_SYMBOL_NODE_URL;
  const repositoryFactory = new RepositoryFactoryHttp(nodeUrl);
  const transactionHttp = repositoryFactory.createTransactionRepository();

  try {
    const transaction = await transactionHttp
      .getTransaction(txHash, TransactionGroup.Confirmed)
      .toPromise();

    return transaction;
  } catch (error) {
    console.error('Failed to fetch transaction:', error);
    return null;
  }
};
```

### ペイロード解析

```typescript
const parseTransactionPayload = (transaction: any): SymbolTransactionPayload => {
  const message = transaction.message;

  if (!message || !message.payload) {
    throw new Error('Invalid transaction: no message payload');
  }

  const payloadJson = message.payload;
  const payload: SymbolTransactionPayload = JSON.parse(payloadJson);

  return payload;
};
```

### ハッシュ比較

```typescript
const calculateContentHash = async (record: LearningRecord): Promise<string> => {
  const content = {
    id: record.id,
    url: record.url,
    title: record.title,
    duration: record.duration,
    understanding: record.understanding,
    note: record.note,
    tags: record.tags,
    createdAt: record.createdAt,
  };

  const sortedKeys = Object.keys(content).sort();
  const sortedContent: Record<string, any> = {};
  sortedKeys.forEach(key => {
    sortedContent[key] = (content as any)[key];
  });

  const jsonString = JSON.stringify(sortedContent);
  const encoder = new TextEncoder();
  const data = encoder.encode(jsonString);
  const hashBuffer = await crypto.subtle.digest('SHA-256', data);

  const hashArray = Array.from(new Uint8Array(hashBuffer));
  const hashHex = hashArray.map(b => b.toString(16).padStart(2, '0')).join('');

  return hashHex;
};
```

---

## インタラクションフロー

### 検証実行

```
1. ユーザーがトランザクションハッシュを入力
   ↓
2. バリデーション実行
   ↓
3. "検証する"ボタンをクリック
   ↓
4. 検証プロセス開始（5ステップ）
   ↓
5. 各ステップの進行状況表示
   ↓
6. 検証完了
   ↓
7. 結果表示（成功 or 失敗）
   ↓
8. ユーザーが追加アクション選択
   - Symbol Explorer確認
   - 新しい検証
   - 詳細表示
```

---

## レスポンシブデザイン

### デスクトップ（1024px以上）
- 入力フィールド: 最大幅 600px
- ProofCard: 最大幅 800px
- ガイドセクション: 2カラム

### タブレット（768px - 1023px）
- 入力フィールド: 画面幅の90%
- ProofCard: 画面幅の95%
- ガイドセクション: 1カラム

### モバイル（767px以下）
- 入力フィールド: 画面幅の100%
- ProofCard: 画面幅の100%
- ハッシュ表示: 折り返し、スクロール可能

---

## エラーハンドリング

### ネットワークエラー
```
Symbol Testnetに接続できません。
インターネット接続を確認してください。
```

### タイムアウトエラー
```
検証がタイムアウトしました。
もう一度試してください。
```

### バリデーションエラー
```
入力が無効です。
64文字の16進数を入力してください。
```

---

このページは **第三者が学習記録の真正性を検証する**ための重要なインターフェースです。信頼性と透明性を視覚的に示すことが重要です。